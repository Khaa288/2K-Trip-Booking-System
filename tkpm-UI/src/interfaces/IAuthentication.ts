interface LoginResponse {
    data?: {
        id: number,
        username: string
    },
    error?: any
}

interface RegisterCustomerResponse {
    data?: any
    error?: any
}

interface RegisterDriverResponse {
    data?: any
    error?: any
}